package factura.factura.repository;

import org.springframework.data.jpa.repository.JpaRepository; 

public interface Detalle_ventaRepository extends JpaRepository<Detalle_ventaRepository, Long>{
    
}